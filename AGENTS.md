# Agent workflow

Before starting work, create a new branch and a dedicated git worktree for it. Do all work in
that worktree. After creating the pull request, remove the worktree.

See [docs/releasing.md](docs/releasing.md) for the release workflow.
